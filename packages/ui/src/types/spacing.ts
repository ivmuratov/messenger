import type { Scale } from "./scale";

export interface SpacingProps {
  ml?: Scale;
  mr?: Scale;
  mt?: Scale;
  mb?: Scale;
  mx?: Scale;
  my?: Scale;
  m?: Scale;
  pl?: Scale;
  pr?: Scale;
  pt?: Scale;
  pb?: Scale;
  px?: Scale;
  py?: Scale;
  p?: Scale;
}

export interface GapProps {
  gap?: Scale;
}
